apiVersion: kubechain.humanlayer.dev/v1alpha1
kind: LLM
metadata:
  name: llm-bad-secret
spec:
  provider: openai
  apiKeyFrom:
    secretKeyRef:
      name: nonexistent-secret
      key: OPENAI_API_KEY
  temperature: "0.7"
