apiVersion: kubechain.humanlayer.dev/v1alpha1
kind: Task
metadata:
  name: fetch-example
spec:
  agentRef:
    name: web-fetch-agent
  message: "Please fetch the content from example.com and summarize what's on the site."
